{
  "name": "map-mover",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "clean:lib": "rm -rf webchat/lib",
    "clean:dist": "rm -rf webchat/dist",
    "build": "npm run clean:dist && SET NODE_ENV=development npm run build:webpack",
    "build:webpack": "node webchat/bin/build.js",
    "build:js": "babel src -d webchat/lib",
  	"start": "npm run clean:dist && node webchat/bin/build-dev"
  }	
}
